"""drc-pay backend API.

Intentionally empty of imports: importing a domain submodule (e.g.
``drc_pay_api.domains.ledger.money``) must not pull in FastAPI or any I/O, so the
pure money logic can be tested with zero external dependencies.
"""
